import { InvoiceUpdateCommand } from './invoice-update-command.model';

describe('InvoiceUpdateCommand', () => {
  it('should create an instance', () => {
    expect(new InvoiceUpdateCommand()).toBeTruthy();
  });
});
